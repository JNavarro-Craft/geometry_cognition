"""sap_developer_server — MCP that consumes the SAP bridge over HTTP.

The first consumer of the bridge (Objetivo 1). Read-only, strictly agnostic: it
relays facts from the bridge to the LLM and adds no structural interpretation.
"""
