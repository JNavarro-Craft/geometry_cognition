[project]
name = "geometry-cognition"
version = "0.1.0"
description = "Modular MCP monorepo for domain-agnostic 3D geometry cognition."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "jsonschema>=4.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
